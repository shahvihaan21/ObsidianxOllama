# Local Assistant - dependencies
#
# main.py, core.py, ollama.py, tools.py and obsidian.py use the standard library
# ONLY. Everything below is needed for voice, plus pytest for the tests.

# --- Speech to text (faster-whisper -> CPU, no torch) ---------------------
faster-whisper>=1.0.3
sounddevice>=0.4.6
numpy>=1.26

# --- Text to speech (Windows SAPI5 via pyttsx3) ---------------------------
pyttsx3>=2.90

# --- Tests ---------------------------------------------------------------
pytest>=8.0