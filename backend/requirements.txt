# Obsidian Control — backend dependencies
#
# app/config.py, app/commands.py, app/obsidian.py, app/llm.py, app/research.py,
# app/router.py and app/voice.py use the standard library only. Everything
# below is either the web API or optional voice input.

# --- Web API + plugin bridge (WebSocket support comes from uvicorn[standard]) --
fastapi>=0.115
uvicorn[standard]>=0.30

# --- Optional: speech to text from a local microphone -------------------------
# Without these three packages the system still runs; the microphone button
# reports that voice input is unavailable.
sounddevice>=0.4.6
numpy>=1.26
faster-whisper>=1.0.3

# --- Tests -------------------------------------------------------------------
pytest>=8.0
