# Local Assistant Backend - dependencies
#
# core.py, ollama.py, tools.py, obsidian.py, commands.py and voice.py use the
# standard library ONLY except where noted. Everything below is for voice, the
# FastAPI server, and tests.

# --- Web API ---
fastapi>=0.115
uvicorn[standard]>=0.30

# --- Speech to text (faster-whisper -> CPU, no torch) ---------------------
faster-whisper>=1.0.3
sounddevice>=0.4.6
numpy>=1.26

# --- Text to speech (Windows SAPI5 via pyttsx3) ---------------------------
pyttsx3>=2.90

# --- Tests ---------------------------------------------------------------
pytest>=8.0
