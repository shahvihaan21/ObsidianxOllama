# Local AI Assistant — dependencies
#
# core modules (assistant.py, llm.py, tools.py, obsidian.py, commands.py)
# use the standard library only. The packages below are optional features.

# --- Speech to text (faster-whisper → CPU, no torch) ----------------------
faster-whisper>=1.0.3
sounddevice>=0.4.6
numpy>=1.26

# --- Text to speech (Windows SAPI5 via pyttsx3) ---------------------------
pyttsx3>=2.90

# --- Tests ----------------------------------------------------------------
pytest>=8.0
